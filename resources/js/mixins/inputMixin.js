export default {
    methods: {
        updateEmail(event) {
            this.email = event
        },
        updateLogin(event) {
            this.login = event
        },
        updatePassword(event) {
            this.password = event
        },
        updateCurrentPassword(event) {
            this.currentPassword = event
        },
    }
}
